---
import { Code } from 'astro:components';
import type { ComponentProps } from 'astro/types';

type CodeProps = ComponentProps<typeof Code>;

interface Props {
  lang: NonNullable<CodeProps['lang']>;
  code: string;
  filename?: string;
}

const { lang, code, filename } = Astro.props;
---

<figure class="codeblock">
  {filename && <figcaption class="codeblock-header">{filename}</figcaption>}
  <div class="codeblock-body">
    <Code
      code={code}
      lang={lang}
      themes={{ light: 'github-light', dark: 'github-dark' }}
      defaultColor={false}
    />
  </div>
</figure>

<style>
  .codeblock {
    margin: 1.75rem 0;
    border-radius: 4px;
    overflow: hidden;
    border: 1px solid var(--border);
  }

  .codeblock-header {
    font-family: var(--mono);
    font-size: 0.7rem;
    color: var(--muted);
    background: var(--surface);
    padding: 0.5rem 1.25rem;
    border-bottom: 1px solid var(--border);
  }

  .codeblock-body :global(pre.astro-code) {
    margin: 0 !important;
    border-radius: 0 !important;
    border: none;
    padding: 1.25rem 1.5rem !important;
    font-size: 0.82rem;
    line-height: 1.65;
  }
</style>
